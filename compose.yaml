services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=habits-manager-api'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432'
